/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import BlackJackContainer from './src/containers/BlackJackContainer';

AppRegistry.registerComponent(appName, () => BlackJackContainer);
