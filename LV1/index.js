/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import JambContainer from './src/containers/JambContainer';

AppRegistry.registerComponent(appName, () => JambContainer);
