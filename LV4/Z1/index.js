/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import BirdContainer from './src/container/BirdContainer';

AppRegistry.registerComponent(appName, () => BirdContainer);
